fixes:
  - "ros_ws/src/*/vortex-vkf/::"

comment:
  layout: "header, diff, flags, components"
 
coverage:
  status:
    project: off
    patch: off

flag_management:
  individual_flags:
     - name: vortex-filtering
       paths:
        - vortex-filtering/include/vortex_filtering/
       statuses:
        - type: project
          target: auto
          threshold: 1% 
